# ============================================================
#  私密内容 —— 绝对不要提交
# ============================================================
#
#  这个项目会保存你和「朋友」的全部对话，以及它对你的记忆档案。
#  这些东西一旦推到 GitHub 就收不回来了（即使后来删除，也留在别人的 fork
#  和 GitHub 的缓存里）。所以下面每一条都是认真的。

# 密钥：DeepSeek API Key、Bark Key、访问口令都在这里面
config.json

# 聊天记录、记忆、人设、头像、图片
data/

# 自动备份（含完整的聊天记录副本）
backups/

# 运行日志（可能含对话内容）
logs/
*.log

# 测试用的临时数据目录
# 测试会把种子数据写在这里。虽然只是测试数据，
# 但测试会复制真实的 persona.md / memory.md 进来——那是私密内容。
test/tmp/

# 编辑器 / 系统垃圾
.vscode/
.idea/
.DS_Store
Thumbs.db
*.tmp
*.swp

# �ύ��Ϣ����ʱ�ļ�
.git-commit-msg.txt
.msg.tmp
